let name = 'rochafi'

// Besarkan karakter pertama, index 0 dg toUpperCase
// Gabungkan dengan karakter kedua sampai akhir
name = name[0].toUpperCase() + name.slice(1)
      // R                   +  ochafi  = Rochafi
console.log(name)